{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^16.4.13",
    "jest": "^27.0.6",
    "lerna": "^4.0.0",
    "prettier": "^2.3.2",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna run tsc && lerna publish"
  }
}
